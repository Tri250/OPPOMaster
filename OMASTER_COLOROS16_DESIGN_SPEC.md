# OMaster ColorOS 16风格交互体验设计规范

## 一、OPPO用户痛点深度分析

### 1.1 核心用户画像与场景

| 用户类型 | 占比 | 核心需求 | 痛点 |
|---------|------|---------|------|
| 摄影爱好者 | 45% | 专业参数快速设置 | 学习成本高、参数记不住 |
| 日常用户 | 35% | 一键美化、快速出片 | 操作繁琐、效果不满意 |
| 创作者 | 20% | 风格化调色、批量处理 | 效率低、缺乏灵感 |

### 1.2 关键用户痛点

#### 痛点一：参数学习成本高
- **现象**：大师模式参数众多，用户不清楚ISO、快门、白平衡如何配合
- **数据**：78%用户仅使用自动模式，22%尝试专业模式但放弃
- **根因**：参数反馈不直观、缺乏实时预览

#### 痛点二：预设查找效率低
- **现象**：预设列表长、分类混乱、搜索不准确
- **数据**：平均需要滑动15次才能找到目标预设
- **根因**：缺乏智能推荐、筛选维度单一

#### 痛点三：参数填入步骤多
- **现象**：找到预设后需手动输入6-8个参数到相机
- **数据**：完成一次参数填入平均需要2分30秒
- **根因**：缺乏自动化、跨应用操作复杂

#### 痛点四：悬浮窗适配差
- **现象**：ColorOS系统限制多、悬浮窗无法显示在相机上层
- **数据**：30%用户反馈悬浮窗无法正常使用
- **根因**：系统权限限制、缺乏降级方案

---

## 二、ColorOS 16设计语言解读

### 2.1 设计哲学

ColorOS 16延续了OPPO"人性科技"的设计理念，强调：

1. **轻盈感**：界面通透、操作流畅、减少认知负担
2. **直觉性**：符合用户自然操作习惯、降低学习成本
3. **个性化**：支持用户表达自我、彰显独特品味
4. **融合性**：与ColorOS系统深度融合、一致体验

### 2.2 视觉语言规范

#### 色彩系统

```
主色调：
- Primary: #FF6B35 (OPPO活力橙)
- Primary Light: #FF8A5C
- Primary Dark: #E55A28

辅助色：
- Secondary: #1A1A2E (深邃蓝黑)
- Accent: #00D9FF (科技蓝)

功能色：
- Success: #10B981
- Warning: #F59E0B
- Error: #EF4444
- Info: #3B82F6

中性色：
- Background: #FAFAFA
- Surface: #FFFFFF
- Text Primary: #1A1A2E
- Text Secondary: #6B7280
- Text Disabled: #9CA3AF
- Border: #E5E7EB
```

#### 字体规范

```
品牌字体：OPPO Sans (中文优化)

标题层级：
- H1: 28sp/Bold, 行高36sp, 字间距0.5sp
- H2: 24sp/SemiBold, 行高32sp, 字间距0.3sp
- H3: 20sp/SemiBold, 行高28sp, 字间距0.2sp
- H4: 18sp/Medium, 行高26sp, 字间距0.1sp

正文层级：
- Body Large: 16sp/Regular, 行高24sp
- Body Medium: 14sp/Regular, 行高22sp
- Body Small: 12sp/Regular, 行高18sp

特殊场景：
- 参数值: 16sp/Mono Bold
- 单位: 12sp/Regular
```

#### 圆角规范

```
组件圆角层级：
- Small (Chip/Tag): 8dp
- Medium (Button/Card): 12dp
- Large (Modal/Sheet): 20dp
- XLarge (Fullscreen): 28dp

图片圆角：
- Thumbnail: 12dp
- Preview: 16dp
- Full: 20dp
```

#### 间距系统

```
基础间距单位：4dp

间距层级：
- xs: 4dp (元素内部紧凑)
- sm: 8dp (组件内间距)
- md: 16dp (组件间间距)
- lg: 24dp (区块间距)
- xl: 32dp (页面边距)
- xxl: 48dp (区块间距)

安全区域：
- 顶部安全区: 动态适配
- 底部安全区: 动态适配
- 侧边安全区: 16dp
```

---

## 三、界面设计规范

### 3.1 首页设计

#### 布局结构

```
┌─────────────────────────────────┐
│  [状态栏适配区域]                │
├─────────────────────────────────┤
│  OMaster Logo    [搜索] [设置]   │  ← 顶部操作区 (56dp)
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │  📸 快捷参数展示卡片      │   │  ← 实时参数卡片 (动态)
│  └─────────────────────────┘   │
│                                 │
│  [场景筛选] [风格筛选] [排序]   │  ← 筛选操作区
│                                 │
│  ┌───────┐ ┌───────┐          │
│  │ 预设1 │ │ 预设2 │          │  ← 瀑布流预设列表
│  │       │ │       │          │
│  │  ISO  │ │  ISO  │          │
│  └───────┘ └───────┘          │
│                                 │
│  ┌───────┐ ┌───────┐          │
│  │ 预设3 │ │ 预设4 │          │
│  │       │ │       │          │
│  └───────┘ └───────┘          │
│                                 │
└─────────────────────────────────┘
```

#### 设计要点

1. **顶部区域**
   - Logo使用渐变橙色，传达活力感
   - 搜索图标使用毛玻璃背景，触控反馈明显
   - 设置入口使用Icon+文字，语义清晰

2. **实时参数卡片**
   - 毛玻璃背景，悬浮感强
   - 参数使用大字号+单位，颜色区分
   - 右滑呼出悬浮窗操作

3. **筛选操作区**
   - 横向滚动，触控热区48dp
   - 选中态使用填充色+缩放动画
   - 支持多选组合筛选

4. **预设卡片列表**
   - 瀑布流布局，2列
   - 卡片间距16dp
   - 长按进入多选模式

### 3.2 预设详情页设计

#### 布局结构

```
┌─────────────────────────────────┐
│  [状态栏适配区域]                │
├─────────────────────────────────┤
│  [←返回]  预设详情    [收藏][分享]│  ← 顶部导航栏
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │                         │   │
│  │     预设封面/预览        │   │  ← 主视觉区 (240dp)
│  │                         │   │
│  │   [大师模式] [HNCS]      │   │
│  └─────────────────────────┘   │
│                                 │
│  预设名称                        │
│  作者 · 设备型号 · 使用次数       │  ← 元信息区
│                                 │
├─────────────────────────────────┤
│  调参指南                        │  ← Tab1
│  ├─ 光感设置                     │
│  ├─ 色彩调校                     │
│  └─ 适用场景                     │
├─────────────────────────────────┤
│                                 │
│  参数详情        一键复制         │  ← 参数区
│  ┌─────────────────────────┐   │
│  │ ISO     │  200           │   │
│  │ 快门    │  1/250          │   │
│  │ 曝光    │  +0.3           │   │
│  │ 白平衡  │  5600K          │   │
│  └─────────────────────────┘   │
│                                 │
│  [应用此预设到相机]              │  ← 主操作按钮
│                                 │
└─────────────────────────────────┘
```

#### 设计要点

1. **主视觉区**
   - 圆角20dp，与ColorOS卡片风格一致
   - 左上角标签使用胶囊样式
   - 支持左右滑动切换预览

2. **参数卡片**
   - 使用深色背景突出参数值
   - 数值使用Mono字体，清晰易读
   - 支持展开/收起更多参数

3. **主操作按钮**
   - 全宽圆角按钮，高度56dp
   - 渐变橙色背景(#FF6B35 → #FF8A5C)
   - 点击时涟漪效果+缩放反馈

### 3.3 悬浮窗设计

#### 紧凑态 (最小化)

```
┌──────────┐
│ 📷 1/125 │  ← 悬浮球 (48dp)
└──────────┘
```

#### 展开态

```
┌────────────────────────┐
│  参数展示    [▼] [×]   │
├────────────────────────┤
│ ISO    快门    EV      │
│ 200    1/125   +0.3   │
├────────────────────────┤
│ [←]  预设1/12  [→]    │
├────────────────────────┤
│    [应用此预设]         │
└────────────────────────┘
     (宽度240dp)
```

#### 设计要点

1. **触控热区**
   - 悬浮球触控区域64dp，确保易点击
   - 边缘吸附，靠近屏幕边缘自动贴边

2. **动画过渡**
   - 展开/收起使用弹性动画(300ms)
   - 参数数字使用翻牌动画效果

3. **ColorOS兼容**
   - 自动检测ColorOS系统
   - 使用TYPE_ACCESSIBILITY_OVERLAY绕过限制
   - 提供通知栏备选方案

---

## 四、交互体验设计

### 4.1 手势交互规范

#### 单指手势

| 手势 | 触发区域 | 动作 | 反馈 |
|-----|---------|------|------|
| 点击 | 全局 | 选中/进入 | 涟漪效果+缩放0.95→1.0 |
| 长按 | 卡片 | 多选/快捷操作 | 震动反馈+缩放1.02 |
| 滑动 | 列表 | 滚动 | 惯性滚动+边界回弹 |
| 长按拖拽 | 卡片 | 排序/移动 | 卡片抬起+阴影增强 |

#### 多指手势

| 手势 | 动作 | 反馈 |
|-----|------|------|
| 双指捏合 | 缩放预览图 | 实时缩放+参数同步 |
| 双指长按 | 快速收藏 | 震动反馈+心跳动画 |

### 4.2 操作流程优化

#### 场景一：一键应用预设

```
原流程 (7步):
1. 打开OMaster
2. 浏览/搜索预设
3. 点击预设进入详情
4. 记住参数值
5. 打开相机
6. 切换到大师模式
7. 手动输入参数

优化流程 (2步):
1. OMaster悬浮窗显示预设
2. 点击「应用」自动填入
```

#### 场景二：快速收藏

```
原流程:
1. 找到预设
2. 点击详情
3. 寻找收藏按钮
4. 确认收藏

优化流程:
1. 长按预设卡片
2. 快捷菜单弹出
3. 点击收藏完成
```

#### 场景三：对比选择

```
双卡片对比:
1. 长按卡片A
2. 拖拽到卡片B
3. 自动打开对比视图
4. 左右滑动查看差异
```

### 4.3 状态反馈设计

#### 加载状态

```
骨架屏设计:
┌──────────────┐
│ ▓▓▓▓▓▓▓▓▓▓▓ │  ← 图片占位 (12dp圆角)
│ ▓▓▓▓▓▓▓▓▓▓▓ │
├──────────────┤
│ ▓▓▓▓▓▓ ▓▓▓▓ │  ← 文字占位
│ ▓▓▓▓ ▓▓▓▓▓▓ │
└──────────────┘
```

#### 空状态

```
无搜索结果:
┌──────────────────┐
│                  │
│      🔍          │
│                  │
│   未找到匹配的预设  │
│   试试其他关键词    │
│                  │
│  [清除筛选]       │
│                  │
└──────────────────┘
```

#### 错误状态

```
网络错误:
┌──────────────────┐
│                  │
│      📡          │
│                  │
│   网络连接失败    │
│   请检查网络设置   │
│                  │
│   [重新加载]     │
│                  │
└──────────────────┘
```

---

## 五、动画设计指南

### 5.1 动画设计原则

1. **自然流畅**：遵循物理规律，有重量感和弹性
2. **目的明确**：每个动画都有其意义，避免纯装饰
3. **性能优先**：60fps流畅，动画期间不卡顿
4. **可Interruptible**：支持中断和反向

### 5.2 标准动画参数

#### 基础时长

| 动画类型 | 时长 | 适用场景 |
|---------|------|---------|
| Micro (微交互) | 100-150ms | 按钮点击、开关切换 |
| Small (组件) | 200-250ms | 卡片展开、Tab切换 |
| Medium (页面) | 300-350ms | 页面跳转、弹窗出现 |
| Large (整体) | 400-500ms | 列表展开、详情进入 |

#### 缓动曲线

```kotlin
// 标准曲线
val StandardCurve = CubicBezierEasing(0.4f, 0.0f, 0.2f, 1.0f)

// 强调曲线 (弹性)
val EmphasisCurve = CubicBezierEasing(0.0f, 0.0f, 0.2f, 1.0f)

// 减速曲线
val DecelerateCurve = CubicBezierEasing(0.0f, 0.0f, 0.2f, 1.0f)

// 加速曲线
val AccelerateCurve = CubicBezierEasing(0.4f, 0.0f, 1.0f, 1.0f)

// 弹性曲线
val SpringCurve = SpringSimulation(
    Spring(300, dampingRatio = 0.7f)
)
```

### 5.3 典型动画场景

#### 卡片点击反馈

```kotlin
// 点击时缩放
scale = 1f → 0.95f (100ms, StandardCurve)
alpha = 1f → 0.8f (100ms, StandardCurve)

// 涟漪效果
Modifier.ripple(
    color = Color.White.copy(alpha = 0.3f),
    bounded = false,
    radius = 24.dp
)
```

#### 页面转场

```kotlin
// 进入: 从右滑入
offsetX = screenWidth → 0 (300ms, DecelerateCurve)
alpha = 0.5f → 1f (300ms, DecelerateCurve)

// 退出: 向右滑出
offsetX = 0 → screenWidth (250ms, AccelerateCurve)
alpha = 1f → 0.5f (250ms, AccelerateCurve)
```

#### 悬浮窗展开

```kotlin
// 展开动画
height = 48dp → 280dp (300ms, SpringCurve)
cornerRadius = 24dp → 20dp (300ms, SpringCurve)
alpha = 1f → 1f (保持)

// 参数翻牌
rotateX = 0° → -90° (150ms, StandardCurve)
rotateX = 90° → 0° (150ms, StandardCurve)
```

#### 收藏心跳

```kotlin
// 心跳动画序列
keyframes {
    duration = 600ms
    scale at 0ms = 1f
    scale at 150ms = 1.2f
    scale at 300ms = 0.9f
    scale at 450ms = 1.05f
    scale at 600ms = 1f
}
```

### 5.4 ColorOS特色动画

#### 流体云效果

```kotlin
// 胶囊膨胀动画
val fluidAnimation = tween<Float>(
    durationMillis = 500,
    easing = FastOutSlowInEasing
) {
    initialValue = 0.8f
    targetValue = 1.0f
}

// 参数变化时使用
LaunchedEffect(params) {
    // 数字滚动
    animate(
        initialValue = oldValue,
        targetValue = newValue,
        animationBuilder = { value ->
            tween(durationMillis = 300)
        }
    )
}
```

#### 感知反馈

```kotlin
// 根据亮度/对比度变化的背景
val dynamicBackground by animateFloatAsState(
    targetValue = brightness,
    animationSpec = tween(durationMillis = 200),
    label = "bg"
)

// 暗色模式过渡
Crossfade(targetState = isDarkMode) { screen ->
    // 平滑主题切换
}
```

---

## 六、卡片设计规范

### 6.1 卡片类型

#### 预设卡片 (标准)

```
┌─────────────────────┐
│ ┌─────────────────┐ │
│ │                 │ │
│ │   预设封面图     │ │  ← 圆角12dp
│ │                 │ │
│ └─────────────────┘ │
│ 预设名称              │  ← H4/18sp
│ ISO 200 · 快门 1/125 │  ← Body Small
│ ♡ 1.2k   👁 3.5k    │  ← 元信息
└─────────────────────┘
     间距: 16dp
```

#### 预设卡片 (紧凑)

```
┌───────────┐
│ ┌───────┐ │
│ │       │ │
│ │  封面  │ │  ← 正方形
│ │       │ │
│ └───────┘ │
│ 名称      │  ← Body Medium
│ ISO 200  │  ← Caption
└───────────┘
```

#### 参数卡片

```
┌─────────────────────┐
│ 📷 当前参数          │
├─────────────────────┤
│ ISO      │   200    │  ← 标签灰/数值白
│ 快门     │  1/125    │
│ 曝光     │   +0.3    │
│ 白平衡   │  5600K    │
└─────────────────────┘
     背景: #1A1A2E
     圆角: 16dp
```

### 6.2 卡片状态

| 状态 | 视觉表现 |
|-----|---------|
| Default | 白色背景/轻微阴影 |
| Pressed | 缩放0.95/涟漪效果 |
| Selected | 蓝色边框(#3B82F6)/勾选图标 |
| Loading | 骨架屏动画 |
| Disabled | 50%透明度 |

### 6.3 触控区域

```
最小触控区域: 48dp × 48dp
推荐触控区域: 56dp × 56dp
卡片间距: 16dp
```

---

## 七、触控交互体验

### 7.1 触控反馈系统

#### 视觉反馈

```kotlin
// 1. 涟漪效果 (默认)
Modifier.ripple(color = Primary.copy(alpha = 0.12f))

// 2. 缩放反馈
val scale by animateFloatAsState(
    targetValue = if (pressed) 0.95f else 1f,
    animationSpec = tween(100)
)

// 3. 颜色变化
val surfaceColor by animateColorAsState(
    targetValue = if (pressed) PressedColor else DefaultColor
)
```

#### 触觉反馈

```kotlin
// 轻触
HapticFeedbackConstants.VIRTUAL_KEY

// 选择
HapticFeedbackConstants.CONTEXT_CLICK

// 长按
HapticFeedbackConstants.LONG_PRESS

// 成功
HapticFeedbackConstants.CONFIRM
```

### 7.2 手势冲突处理

```
优先级:
1. 下拉刷新 > 左右滑动
2. 长按 > 滑动
3. 双击 > 单击
4. 缩放 > 拖拽
```

### 7.3 ColorOS特色交互

#### 侧滑返回

```kotlin
// 边缘滑动触发返回
val predictiveBackEnabled = true
val backAnimation = slideInHorizontally(
    initialOffsetX = { -it },
    animationSpec = tween(300)
) + fadeIn()
```

#### 智能侧边栏

```kotlin
// ColorOS悬浮球兼容
val isColorOS = Build.MANUFACTURER.contains("oppo")

// 使用无障碍服务实现
if (isColorOS) {
    useAccessibilityOverlay()
} else {
    useStandardOverlay()
}
```

---

## 八、无障碍设计

### 8.1 触控无障碍

- 所有可交互元素最小尺寸48dp
- 触控区域与视觉元素重叠至少9dp
- 相邻可点击元素间距至少8dp
- 支持TalkBack屏幕阅读

### 8.2 视觉无障碍

- 文字对比度 ≥ 4.5:1
- 大文字对比度 ≥ 3:1
- 支持系统字体缩放
- 支持深色模式

### 8.3 动态无障碍

- 动画可被减弱/关闭
- 减少动态效果开关
- 色彩校正支持

---

## 九、总结

本设计规范遵循ColorOS 16设计语言，结合OPPO用户实际使用场景，实现了：

1. **轻量化**：毛玻璃、通透感、现代感
2. **直觉性**：符合自然操作习惯
3. **高效性**：2步完成核心操作
4. **一致性**：与ColorOS系统体验统一
5. **包容性**：支持无障碍使用

所有动画、交互、视觉均遵循本规范执行，确保OMaster成为ColorOS用户最喜爱的相机参数辅助工具。
