# 小O帮帮 - 构建和发布指南

## 📦 构建说明

### 环境要求

#### 系统要求
- **操作系统：** macOS 12+ / Ubuntu 20.04+ / Windows 10+
- **内存：** 至少 8GB RAM（推荐 16GB）
- **磁盘空间：** 至少 10GB 可用空间

#### 开发工具
- **JDK：** OpenJDK 17 或更高版本
- **Android Studio：** Android Studio Hedgehog (2023.1.1) 或更高版本
- **Gradle：** 8.5 或更高版本（项目已包含 Gradle Wrapper）
- **Android SDK：** API 36 (Android 16)

### 构建步骤

#### 1. 克隆项目
```bash
git clone https://github.com/your-org/omaster.git
cd omaster
git checkout trae/solo-agent-w3ei06
```

#### 2. 配置 Android SDK
确保 Android SDK 已正确安装并配置：

```bash
# 在项目根目录创建 local.properties 文件
echo "sdk.dir=/path/to/android/sdk" > local.properties

# 或在 Android Studio 中自动配置
```

#### 3. 同步 Gradle
```bash
# 使用项目自带的 Gradle Wrapper
./gradlew --version          # 验证 Gradle 版本
./gradlew dependencies      # 检查依赖
./gradlew assembleDebug     # 编译 Debug 版本
```

#### 4. 构建 Debug 版本
```bash
./gradlew assembleDebug
```

构建完成后，APK 文件将生成在：
```
app/build/outputs/apk/debug/app-debug.apk
```

#### 5. 构建 Release 版本
```bash
# 配置签名密钥（首次需要）
# 创建 keystore.properties 文件
storePassword=your_store_password
keyPassword=your_key_password
keyAlias=omaster
storeFile=/path/to/your/keystore.jks

# 构建 Release APK
./gradlew assembleRelease
```

#### 6. 安装和测试

**方式一：通过 Android Studio**
1. 打开项目
2. 选择 `app` 模块
3. 点击 Run → Run 'app'

**方式二：通过命令行**
```bash
# 连接设备或启动模拟器
adb devices

# 安装 Debug APK
adb install app/build/outputs/apk/debug/app-debug.apk

# 启动应用
adb shell am start -n com.omaster.app/.MainActivity
```

---

## 🧪 测试指南

### 单元测试
```bash
# 运行所有单元测试
./gradlew test

# 运行特定测试类
./gradlew test --tests "com.omaster.app.model.*"

# 生成测试报告
./gradlew testDebugUnitTest
open app/build/reports/tests/testDebugUnitTest/index.html
```

### UI 测试
```bash
# 运行所有 UI 测试
./gradlew connectedAndroidTest

# 运行特定测试类
./gradlew connectedAndroidTest --tests "com.omaster.app.ui.screens.*"
```

### 代码覆盖率
```bash
# 生成覆盖率报告
./gradlew testDebugUnitTest jacocoTestReport
open app/build/reports/jacoco/testDebugUnitTest/index.html
```

---

## 🔧 常见问题解决

### 问题 1：Gradle 构建失败

**症状：** `./gradlew` 命令执行失败

**解决方案：**
```bash
# 清理 Gradle 缓存
./gradlew clean

# 删除 .gradle 和 build 目录
rm -rf .gradle build app/build

# 重新同步
./gradlew --refresh-dependencies
```

### 问题 2：缺少 Android SDK

**症状：** `SDK location not found` 错误

**解决方案：**
1. 在 Android Studio 中打开 Project Structure (⌘+;)
2. 选择 SDK Location
3. 设置 Android SDK 路径
4. 或创建 `local.properties` 文件：
```properties
sdk.dir=/Users/your-username/Library/Android/sdk
```

### 问题 3：依赖下载缓慢

**症状：** 构建过程中依赖下载非常慢

**解决方案：**
```properties
# 在 gradle.properties 中添加镜像源
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# 使用阿里云镜像（在中国大陆）
allprojects {
    repositories {
        maven { url 'https://maven.aliyun.com/repository/google' }
        maven { url 'https://maven.aliyun.com/repository/public' }
        maven { url 'https://maven.aliyun.com/repository/gradle-plugin' }
        google()
        mavenCentral()
    }
}
```

### 问题 4：内存不足

**症状：** 构建过程 OOM (Out Of Memory)

**解决方案：**
```properties
# 在 gradle.properties 中增加内存
org.gradle.jvmargs=-Xmx4096m -XX:+HeapDumpOnOutOfMemoryError
```

### 问题 5：Kotlin 编译错误

**症状：** Kotlin 代码编译失败

**解决方案：**
```bash
# 清理并重新编译
./gradlew clean
./gradlew :app:compileDebugKotlin
```

---

## 📱 发布流程

### 1. 准备阶段

#### 检查清单
- [ ] 所有功能已完成并测试通过
- [ ] UI/UX 设计符合 ColorOS 16 规范
- [ ] 安全和隐私检查通过
- [ ] 性能指标达到要求
- [ ] 文档已完成

#### 代码审查
```bash
# 检查代码风格
./gradlew ktlintCheck

# 检查代码质量
./gradlew lint
```

### 2. 版本管理

#### 更新版本号
编辑 `app/build.gradle.kts`：
```kotlin
versionCode = 122  // 递增版本代码
versionName = "1.2.2"  // 更新版本名称
```

#### Git 标签
```bash
git tag -a v1.2.1 -m "Release version 1.2.1"
git push origin v1.2.1
```

### 3. 构建产物

#### Debug APK
```bash
./gradlew assembleDebug
# 输出：app/build/outputs/apk/debug/app-debug.apk
```

#### Release APK
```bash
./gradlew assembleRelease
# 输出：app/build/outputs/apk/release/app-release.apk
```

#### Bundle (Google Play)
```bash
./gradlew bundleRelease
# 输出：app/build/outputs/bundle/release/app-release.aab
```

### 4. 应用签名

#### 创建签名密钥
```bash
keytool -genkey -v -keystore omaster.jks -keyalg RSA -keysize 2048 -validity 10000 \
  -alias omaster -storepass your_store_password -keypass your_key_password \
  -dname "CN=OMaster, OU=Development, O=OMaster, L=Beijing, ST=Beijing, C=CN"
```

#### 配置签名
在 `gradle.properties` 中配置：
```properties
OMASTER_STORE_FILE=omaster.jks
OMASTER_STORE_PASSWORD=your_store_password
OMASTER_KEY_ALIAS=omaster
OMASTER_KEY_PASSWORD=your_key_password
```

---

## 📊 发布检查清单

### 功能检查
- [ ] 首页预设列表正常显示
- [ ] 预设详情页参数展示完整
- [ ] 搜索和筛选功能正常
- [ ] 收藏功能正常
- [ ] 主题切换功能正常
- [ ] AI场景识别功能正常
- [ ] AI微调功能正常
- [ ] 水印编辑功能正常
- [ ] 悬浮窗功能正常
- [ ] 设置页面功能正常

### 性能检查
- [ ] 应用启动时间 ≤ 2秒
- [ ] 场景识别响应时间 ≤ 300ms
- [ ] AI微调响应时间 ≤ 3秒
- [ ] 内存占用 ≤ 200MB
- [ ] 无明显卡顿和掉帧

### UI/UX 检查
- [ ] 深色/浅色模式切换正常
- [ ] 动画流畅无卡顿
- [ ] 文字清晰可读
- [ ] 图标显示正常
- [ ] 布局适配不同屏幕尺寸

### 安全检查
- [ ] 无敏感信息泄露
- [ ] 网络请求使用 HTTPS
- [ ] 权限请求合理
- [ ] 日志已禁用（Release版本）

### 兼容性检查
- [ ] 在 OPPO Find X8 Ultra 上测试通过
- [ ] 在 OnePlus 13 Pro 上测试通过
- [ ] 在 realme GT7 Pro 上测试通过
- [ ] 在 Android 16 上测试通过
- [ ] 在 Android 8.0 上测试通过

---

## 🚀 Google Play 上架指南

### 1. 创建开发者账号
1. 访问 [Google Play Console](https://play.google.com/console)
2. 支付注册费用（$25）
3. 完成开发者信息填写

### 2. 准备应用信息

#### 应用名称
```
小O帮帮 - OPPO哈苏影像系统
```

#### 应用描述（中文）
```
小O帮帮是一款专为OPPO/一加/realme手机用户设计的专业摄影辅助应用。

✨ 核心功能：

📷 HNCS认证预设
基于OPPO Find X8 Ultra的哈苏大师模式，提供专业级摄影参数预设，覆盖人像、风景、夜景、美食等多种场景。

🤖 AI智能场景识别
智能识别拍摄场景，自动推荐最佳哈苏预设，让每一张照片都成为大师之作。

🎨 AI样张微调
导入您的照片，AI将基于哈苏色彩科学进行智能优化，一键提升照片质量。

💧 ColorOS 16设计
采用ColorOS 16水生设计语言，简洁大气，操作流畅。

🔧 专业相机参数
实时显示相机参数，支持一键复制，方便学习和交流。

📱 悬浮窗功能
全局显示相机参数，随时查看当前拍摄设置。

使用小O帮帮，让您的手机也能拍出专业级照片！
```

#### 应用图标
- 尺寸：512x512 PNG
- 格式：32-bit PNG with alpha
- 文件大小：不超过 1024KB

#### 截图
需要准备以下截图（手机和平板）：
- 手机截图：1080x1920 PNG
- 平板截图：1800x1200 PNG

### 3. 上传应用包
1. 访问 Google Play Console
2. 选择「创建应用」
3. 选择「正式版」发布轨道
4. 上传 `.aab` 文件

### 4. 内容分级
完成问卷调查以确定应用内容分级

### 5. 定价与分发
1. 设置应用为免费
2. 选择分发国家/地区
3. 配置目标设备

### 6. 提交审核
1. 完成所有必填项
2. 点击「提交审核」

---

## 📞 技术支持

### 反馈渠道
- **抖音：** 搜索"带娃的小陈工"
- **小红书：** 搜索"带娃的小陈工"
- **邮箱：** support@omaster.app
- **GitHub Issues：** https://github.com/your-org/omaster/issues

### 常见问题 FAQ
访问我们的官网查看常见问题解答

---

**构建和发布指南版本：** 1.0  
**最后更新：** 2026-05-31
